<h1><%= @user.first_name %>'s Items</h1>
<hr>

<table class="table table-striped table-bordered table-condensed">
  <tr>
    <th>Item</th>
    <th>Quanity</th>
  </tr>

  <% @user.items.each do |item| %>
  <% end %>
</table>

<hr>

<div class="pull-left">
  <%= link_to "Add An Item!", new_my_trip_path, :class => "btn btn-primary" %>
</div>
